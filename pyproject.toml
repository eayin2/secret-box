[project]
name = "encryptmasterpassword"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cryptography>=45.0.6",
    "scrypt>=0.9.4",
]
